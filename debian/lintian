# Shorewall needs to continue in runlevel 1 because networking may still be up
shorewall-init: init.d-script-possible-missing-stop
# The string "/usr" only occurs in the script because it is in a help message
# directing the user to documentation
shorewall-init: init.d-script-missing-dependency-on-remote_fs
